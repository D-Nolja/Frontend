import { defineStore } from "pinia";
import { ref } from "vue";

export const usePlaceState = defineStore("headerStateStore", () => {
    const p

    const getPlaces =  async () => {

    };


  return {};
});
